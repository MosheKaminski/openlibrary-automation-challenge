# Copy to .env locally (never commit real secrets).
# Internet Archive email used as Open Library username.
OL_EMAIL=
OL_PASSWORD=

# Output path for Playwright session (created by: python scripts/save_storage_state.py)
STORAGE_STATE_PATH=storage_state.json

# Optional: 0 to watch the browser during save_storage_state
PLAYWRIGHT_HEADLESS=1
